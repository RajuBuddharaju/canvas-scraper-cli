{
  "name": "canvas-scraper",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "dev": "node index.js",
    "start": "./dist/canvas-scraper.exe",
    "build": "nexe -t win-x64-latest index.js -o dist/canvas-scraper --build && nexe -t linux-x64-latest index.js -o dist/canvas-scraper-linux --build && nexe -t mac-x64-latest index.js -o dist/canvas-scraper-mac --build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "commander": "^11.1.0",
    "dotenv-flow": "^4.1.0",
    "p-limit": "^5.0.0",
    "puppeteer": "^21.9.0"
  }
}
